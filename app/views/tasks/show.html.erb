<div class="card p-4 shadow-sm">
  <h1 class="card-title"><%= @task.title %></h1>
  <p class="card-text text-muted"><%= @task.details %></p>

  <hr>

  <p>
    <% if @task.completed? %>
      <input type="checkbox" checked disabled class="me-2">
      <strong class="text-success">This task is completed</strong> 🎉
    <% else %>
      <input type="checkbox" disabled class="me-2">
      <strong class="text-warning">This task is not completed yet</strong> ⏳
    <% end %>
  </p>
</div>

<div class="mt-3">
  <%= link_to 'Back to List', tasks_path, class: "btn btn-secondary m-2" %>
  <%= link_to 'Edit task', edit_task_path(@task), class: "btn btn-primary" %>
</div>
